<dl>
  <dt>Nombre</dt>
  <dd><%= @user.name %></dd>

  <dt>Inicio de sesión</dt>
  <dd><%= @user.login %></dd>

  <dt>E-mail</dt>
  <dd><%= @user.email %></dd>

  <dt>Acceder a cuenta</dt>
  <dd><%= @user.login_count %></dd>

  <dt>Última petición</dt>
  <dd><%= @user.last_request_at %></dd>

  <% if @user.last_login_at %>
    <dt>Último inicio de sesión</dt>
    <dd><%= @user.last_login_at %></dd>
  <% end %>

  <dt>Inicio de sesión normal</dt>
  <dd><%= @user.current_login_at %></dd>

  <% if @user.last_login_ip %>
    <dt>Última IP de inicio de sesión</dt>
    <dd><%= @user.last_login_ip %></dd>
  <% end %>

  <dt>IP de inicio de sesión normal</dt>
  <dd><%= @user.current_login_ip %></dd>
</dl>
<p style= "margin-left: 43%;"><%= link_to 'Editar', :action => 'edit' %></p>
<div style="margin-left: 48%;"><%= image_tag "login.png", :alt => "Login", :title => "Login" ,  :width => 20 %></div>
